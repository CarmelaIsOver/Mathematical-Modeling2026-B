# 第三轮优化报告（B题-team-pi @ opt/v2）

- 工作目录：`E:\KiriTong\CUMCM2026\B题-team-pi`，分支 `opt/v2`
- 起点：`student-opt-v1`（`56d3e91` / `7119afb`）
- 本轮结论：**1 项采用（Q3 60m 小区域试探）+ 6 项否决**，新标签 **`student-opt-v2`**，未推送 GitHub

## 1. 当前最优配置（本轮更新）

```bash
# Q3：受保护缩环 + 60m 小区域光学试探（本轮新增第二个开关）
python run.py --mode official --problem 3 --robot-id <队号> \
              --q3-ring-guard aggressive --q3-probe-radius 60 --output practice_logs

# Q4：默认（默认已含 40m 小区域试探）；可选加网络健康探测
python run.py --mode official --problem 4 --robot-id <队号> \
              --http-health-probe --output practice_logs
```

不传任何开关时与队友原始基线**逐位一致**（冻结轨迹测试守护）。

| 配置 | 均值秒/源 | 相对 off | P95 | 最差绝对 | 失败 |
|---|---:|---:|---:|---:|---:|
| off（队友基线） | 279.23 | — | 342.30 | 380.08 | 0 |
| **aggressive + probe60** | **239.54** | **−14.22%** | **289.80** | **354.22** | **0** |

（200 个全新种子 3486000–3486199，生产入口 `run.py` offline；194/200 更快，1/200 >5%，最差比 1.0594，CVaR95 比 1.0134。）

## 2. 本轮采用（A7 / A8 / A9）

| # | 项 | 证据 |
|---|---|---|
| **A7** | **Q3 小区域光学试探 60m**（`--q3-probe-radius 60`） | 200 个全新种子（3484000–3484199）配对：241.65 → **239.92 秒/源（−0.74%）**，中位比 0.9933，P95 比 1.0001，CVaR95 比 1.0018，最差比 1.0050，测量 101.8，**0 失败、0 场 >5%**；`probe_radius=40` 同时验证为 −0.67%（尾部略好），两档都在噪声内 |
| A8 | Q4 网络健壮性：既有重试已验证 + 新增 opt-in TCP 健康探测 | `test_http_retry.py` 6 项：10053 族（ConnectionAbortedError/ConnectionResetError/URLError/IncompleteRead）确实被重试且 **request_id 与请求体逐字节一致**；HTTPError 不重试；`--http-health-probe` 为纯 TCP 预检 |
| A9 | Q4 小区域试探窗参数化（默认仍 40，逐位不变） | `--q4-probe-radius`；顺带反向证明现有 40m 试探有价值：关掉后 48 场 −0.92% |

**A7 完备性论据**（不以实测代替证明）：试探失败不签发任何证书、不缩小可行域，仅消耗 3s 光学时间；
失败后仍执行原有证书路径（`certified_clear` 需顶点界 ≤19.99m），故清除完备性与队友基线完全一致。
生产移植对拍：20 种子 × 40/60 两档，**320 个行为字段 0 不一致**。

## 3. 本轮否决（R28–R33）

| # | 方向 | 结果 | 否决原因 |
|---|---|---|---|
| R28 | **方向 C：Q4 证书站点集（7 站只做缺失证明）** | 几何验证：`all25` 未覆盖 0 点；**25 个单站移除每个都产生 ≥972 个 10m 网格点（≥97,200 m²）未覆盖**，0/25 可删；自然 7 站集未覆盖 78%–100% 盘面 | **结构性不可行**。Q4 源有方向性（`visible=(p−源)·dir≥0`），可躲进任一开半平面，因此缺失证明要求"每个盘面点都在其 1000m 内站点的凸包内部（角度间隙<180°）"，该判据下最小证书集 = 全集 25。用 Q3 的"圆盘覆盖"算 Q4 会得出错误结论（中心+6×1645 的圆盘覆盖半径 950m<1000m，看似可行） |
| R29 | 方向 E：覆盖证明提前关闭 | 与 R28 同判据 | 重复方向（同 R23 结论）：不存在可提前关闭的频道 |
| R30 | 方向 F：试探加"≥2 次观测"门 | 200 场与无门版本**逐位相同** | 门从不生效（试探触发时观测数已≥2） |
| R31 | 方向 F2：区域尺度相关动态 skip 阈值 | 48 场与 control **逐位相同** | 站点扫描时刻已知目标的区域半径基本 >300m，阈值退化为静态 1200 |
| R32 | 方向 D：Q4 试探窗 40→60/80 | 48 场：491.44 → 492.03（−0.12%）/ 492.58（−0.23%） | 规则 3：<0.5% 即否决，不调参重试 |
| R33 | 自主迭代：Q4 紧凑目标站点跳过阈值 40→60/80 | 48 场：476.35 → 476.42（−0.013%）/ 476.49（−0.028%） | 规则 3：均值零效果 |

方向 A（Q3 试探放宽）与台账 R6（probe30/40 为 +0.17%/+0.34%）的关系已在台账显式记录：
R6 测的是**无缩环基线**上的试探，本轮测的是**当前最优基线**上的 40/60 两档，因此不是重复实验。

## 4. 收尾验证

- 全量测试：`python -m unittest discover -p "test_*.py"` → **86 项通过**（69 原有 + 7 `test_q3_probe` + 4 `test_q4_probe` + 6 `test_http_retry`）
- 几何验证脚本：`geo_cert_subset_q4.py`（圆盘覆盖，含 C(25,7) 全枚举必要条件）、`geo_cert_hull_q4.py`（凸包判据，10m 网格 + sanity）
- 生产 200 场确认：见 §1
- 无推送；`student-opt-v2` 标签打在本地 `opt/v2`

## 5. 下一步建议

1. **Q3 已接近结构下界**：测量 102.5 次/场已接近"每缺一个频道需 7 站全覆盖"的下界；剩余成本主体是移动，而移动侧候选（R3/R4/R5/R20/R21/R27）已全部否决。建议停止在 Q3 继续找空间。
2. **Q4 的两类站点级旋钮（试探窗、紧凑目标跳过）已测尽**（R32/R33），且方向 C/E 已几何否决。若仍要压 Q4，只剩"非站点级"的定位/清除几何，属高风险区。
3. **比赛实用提示**：Q4 加 `--http-health-probe` 可规避 015014 那样的 v=0 连接中止；Q3 用 `--q3-ring-guard aggressive --q3-probe-radius 60`。
4. 若正式比赛更看重"不出现单场恶化"，可把 Q3 换成 `--q3-ring-guard conservative`（机制更保守，代价约 3.5pp 均值）。

## 6. 产物索引

| 产物 | 位置 |
|---|---|
| Q3 probe 粗筛/200 场/生产对拍/生产确认 | `results/r27_q3_screen48`、`r31_q3_afF2_screen48`、`r32_q3_probe200`、`r33_q3_probe_prod`、`r35_q3_best200_prod` |
| Q4 probe / 紧凑跳过 粗筛 | `results/r34_q4_probe48`、`r36_q4_skiptight48` |
| 方向 C 几何验证 | `geo_cert_subset_q4.py`、`geo_cert_hull_q4.py`、`results/r30_q4_cert_geometry/` |
| 测试 | `test_q3_probe.py`、`test_q4_probe.py`、`test_http_retry.py` |
| 台账 / 逐轮记录 / 报告 | `OPTIMIZATION_LEDGER.md`、`research/iterative_speed/{RESULTS,JOURNAL}.md`、本文件 |
